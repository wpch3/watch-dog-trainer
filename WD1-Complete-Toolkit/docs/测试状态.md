# v0.4.1 更新（2026-09-15）

- 用户新验证：**原生无限电池**生效（与此前原生加金钱合计，原生路线两项通过）。
- UI4 修复：PING/STATUS 计数改走通知通道（宿主面板文字不实时刷新）；全部拒绝通知附带下一步指引；07 页加步骤提示；新增 `docs/使用指引-逐步操作.md`。
- 弹药未生效判定为 2 小时回执门槛（设计行为）；专注待用户按新提示重试后回报。
- 测试：五套 Lua + 验证器全绿（test_ui4 增至 1372 断言，新增拒绝指引/计数通知断言）。

# v0.4.0 用户实测更新（2026-09-15）

**lua.god 首次真实游戏验证通过**：用户开启 God Mode 后，在"大电箱面对面爆炸"这一脚本性必死场景中存活。完整链路（UI4 载入 → ARM → 勾选框 → OnUpdate 维持 → ActivateInvincibility）在 build 11241563 上确认有效，详见 `用户实测-20260915-上帝模式生效.md` 与 `profiles/steam-11241563-god-success-20260915.json`。其余开关与全部解锁批次仍未实测；无敌为会话效果，不存档、不持久。

# v0.4.0 测试状态

## 已运行（2026-09-15，Linux + LuaJIT 2.1，Lua 5.1 语义）

- **test_plugin.lua 1259 断言**：核心守卫、票据、队列、回滚、DLC 隔离、2709 条目录上的批次行为、4096 项上限、ALL ITEMS 不含技能点、spawn/heat/time、probe 不导出隐私。
- **test_ascii_menu.lua 21875 断言**：目录 2709 条逐条字段/ASCII 校验，zh-CN 对照目录同构（2709 条，字段一致）。
- **test_ui2.lua 142 / test_ui3.lua 100**：延迟布局、控件失败恢复、零上值回调、死核 STATUS、旧按钮不能复活死核。
- **test_ui4.lua 1367（新增）**：模拟宿主拒绝重注册时回调仍被无条件重绑（9/9）；**删除全部回调后仅靠轮询完成 ARM→God→维持→还原→STOP 全链路**；一次用户动作恰好一条 UI4_ENTER（fired 去重）；点动勾选框自动复位；无 loadstring 沙箱退化为闭包不崩；[ALL PROGRESSION] 含罪恶值/任务XP/ctOS/小游戏；dlc01 条目全部 DLC-only。
- **validate_catalogs.py**：v0.4 契约（181 行计划、2709 条目录、payload==src 字节一致、UI4 字符串、批次上限、组计数）。

日志见 `release-tests/`（lua-*.log / csharp 未改动不重编）。

## 没有运行

- 没有用户 Windows 上的新插件实测；没有真实游戏 AOB/写入/存档效果认证；没有 AddItem 永久性/衣柜/叫车目录可用性认证。
- 桌面 EXE 未改动（C# 无变化），沿用 0.3.3 已编译产物；安装流程不变。

# v0.3.3 测试状态

## 已运行

- Linux / Mono mcs（C# 7.2）编译器，显式引用 Microsoft .NET Framework 4.8 参考程序集，生成 Windows x64 `WD1-Toolkit.exe`；无平台成就程序集、对应源目录或账号连接。
- **47 项文件/备份核心检查**：保留 v0.2 的安全复制、哈希、备份验证、恢复前快照、模拟失败回滚、只读目标、路径/归属/未知版本拒绝、卸载与真实发行 IL 门槛检查。
- **51 项原生模拟进程检查**：通配 AOB、多匹配上限、rel32 距离和填充、五类已允许寄存器捕获、零写入连接、授权失败、原字节核对、独立代码启停、饮酒两处路径、公共库存保护、忙线程拒绝、外来补丁拒绝、授权过期恢复、对象新鲜度/稳定性、更换对象停止、最低值与增量分离、声誉互斥/上下限、通缉只写16位、容量电池、NaN拒绝、按捕获值冻结而非停游戏、模式切换和先恢复后释放。
- **335 个 Lua 断言**：原有默认关闭/ARM/备份回执/原生签名/无效实体/暂停/卸载/队列保护等；新增全武器、全衣服、全物品批次、逐物品不混入技能点、DLC隔离、单次弹药不会开启无限弹药、无限弹药仍可单独启用。数字包含逐目录条目断言，不是335个独立功能。
- 两个真实 WinForms 窗口在 Linux Mono/Xvfb 显示/关闭；未连接真实游戏，未触发原生捕获/写入。

日志：`release-tests/csharp-v03.log`、`release-tests/lua-v03.log`、`release-tests/results.json`。

## 修复后增加的兼容性验证

用户v0.3已在Windows运行到参数面板入口并报告缺失方法；旧EXE用微软net48元数据审计复现1项缺失，修复后607个引用全部解析。参见 `修复记录-0.3.1.md`。这仍不是修复后的Windows运行认证。

## 没有运行

- 没有用户 Windows 11 上的新程序运行测试。
- 没有真实游戏 AOB 命中、原生跳转、线程上下文/暂停、DLL ABI 或写入测试。
- 没有真实资源值、技能点、车辆身份、活动计时器、服装/武器持久性或游戏内进度的效果认证。
- 没有“全物品无遗漏”的实际游戏目录/前置/存档验证。
- 没有自动识别真实玩家/活动/联机会话；稳定指针和手选模式不是身份认证。

用户以前已返回 Windows PS5.1 的只读诊断；旧检测器源码及其历史39项PS7测试记录没有因此变成新引擎的实测。

## 本次特别检查的用户约束

- 平台成就模块在新源码/二进制/发布依赖中移除。
- 无限库存资源、无限金钱/弹药等与增加资源/金钱/技能点各自独立；增量不启用无限。
- 公共库存保护公开说明含材料/道具/备弹，不冒充材料专用或每材料独立开关。
- 全武器/全衣服/全物品为三个独立批次，先确认，按模式过滤，成长奖励不混入全物品。
- 未绑定项在169条当前计划与142条原游戏验收中保留，不删除以提高完成率；不能假启用。

## 必须继续实际验收

1. 用户正常进入单机，只读扫描并导出 `WD1-Native-Probe.json`；先确认命中数量与原字节。
2. 真实存档备份后，逐项捕获并观察原始值是否对应当前目标；先测低风险小增量。
3. 每个开关测试开启、关闭、热键、模式切换、角色/对象变化、授权到期与正常断开。
4. 数值回读后核对游戏界面/行为，再正常保存、完全退出、重启核对。
5. 本体、DLC、每个数字之旅和小游戏分别记录，不能用一项成功推广到所有场景。
6. 发生原生异常、代码无法恢复或工具崩溃时，完全退出游戏再重启；不要在未恢复状态继续联机。

**原生 pcall / C# try-finally / 模拟回滚不是绝不崩溃或绝不坏档的保证。** 原生层首次实测仍然必需。

## 0.3.2新增扫描检查

48项合成PE/页面/分块回归通过（release-tests/csharp-v032.log），包含70MiB有效可执行节、报告模块大小不一致、非法节、跨块模式去重、不完整扫描禁写、失败后无Engine也可导出。新框架引用审计682项、缺失0。用户这次触发旧检查的具体数值还未获得，不能以合成测试冒充其真实扫描通过。

## 0.3.3 捕获/暂停/排队检查

`tests/Test-Capture.cs`新增39项，见 `release-tests/csharp-v033.log`。模拟暂停5秒后显示缓存但即时写入仍拒绝，预置/排队直到同对象新心跳才执行；超时/取消/对象变化/授权变化不补写。缓存预览不读旧指针，报告不包含原始地址。框架成员引用728项全部解析。先前0.3.2真实扫描成功不代表捕获成功；用户当前具体失败原因仍未由捕获数据确认。
