# 本地文件路线——车辆解锁（2026-09-16 立项）

## 结论
- 脚本运行时路线已穷尽并判死（见 profiles/*-item-grant-no-unlock / spawn-ownership-gate / crawl 记录）。
- **车辆"叫车可用性"是打包在游戏数据库里的配置数据**，随 patch.fat/dat 覆盖加载——不改存档、不依赖原生调用。

## 证据链
- Nexus mod #7 "Ultimate Car On Demand"（2014-07，1.1 版，1135 推荐，4.6万+ 下载）：
  仅两个文件 patch.dat + patch.fat 放入 data_win64 即解锁警车/Madness 等隐藏车。
- 我们的全部运行时负结果与其完全自洽（游戏根本不通过脚本暴露所有权写入路径）。

## 路线
1. 【立即可做】用户自行从 Nexus 下载 UCOD v1.1，patch.dat/patch.fat 放入 data_win64（先备份已有 patch 文件）。
2. 【全解锁版】用户提供该文件后：解析 fat/dat 与其中车辆可用性表布局 → 生成自有补丁，
   覆盖全部 74 辆目录车 + 完整隐藏车清单 → 并入安装器（备份/恢复流程）。
3. 与其他 fat/dat 类 mod（theworse 等）共存需档案合并；单独使用直接覆盖。

## 红线
- 仅本地文件、离线使用；不捆绑任何 exe；安装器继续走备份/恢复。
