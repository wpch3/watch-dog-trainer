# 用户实测 — 2026-09-16：线上限定武器本地解锁成功（v0.4.2）

## 用户回报

> SG-90 / Spec Ops Vector / OCP-11 / MP-9mm 都出现了

## 判定

**lua.unlock.online_gated_weapons 真实游戏验证通过。** 四把武器的官方获取条件分别是：
线上跟踪成功 1 次（SG-90）、线上跟踪 5 次（Spec Ops Vector）、线上骇入 1 次（OCP-11）、
线上骇入 5 次（MP-9mm）。全部仅凭单机 AddItem 授予后直接出现在游戏中。

## 由此确立的结论

1. **"线上进度奖励本地解锁"对背包类物品成立**（武器类）。此为项目的核心承诺之一，首次得到游戏内证据。
2. AddItem → 背包/武器解锁 链路在用户构建上完全打通（此前已有无敌、金钱、电池）。
3. OL-01~OL-04（online_rewards.csv 前四项）标记为 USER_VERIFIED。

## 同轮反馈的否定结果

- 07 / Progression Rewards 全部条目点击+CONFIRM 后无可见效果（回传显示批次确实提交：BATCH_SUBMITTED_NOT_VERIFIED）。
- 判定：技能点/经验/APS令牌**不是背包物品**，AddItem 路径对计数器类进度无效。技能点/经验的正确路径为原生面板资源对象（+9C 金钱 / +A0 技能 / +A8 经验同对象），用户已有该捕获的成功经验（加钱）。
- 0.4.3 已在 07 页面标注此边界，并把通知数字改为整数格式（此前显示 1.000000）。
- APS 令牌需一次"授予后重启"的复核；仍无效果则该组停用待原生路径，不删除条目。

## 下一步

1. 原生面板：捕获钱包 → "增加普通技能点" +1 → 技能页核对（4a 节）。
2. 加经验 +500 → 等级/经验条核对。
3. APS 令牌重启复核回报。


## 补记（同日稍晚）

用户再次尝试加技能点，回传文案为 Lua 格式（Prepared/[CONFIRM]/Batch submitted）——证明操作的仍是游戏内菜单的技能点**物品**（预期无效，判定同上），并未走到桌面原生面板的"增加普通技能点"。v0.4.5 已：03 页按钮改为 "[ITEM ONLY]" 明示；指引 4a 节写入"两个窗口"区分表与判断口诀（消息含 Prepared/CONFIRM = 走错窗口）。
