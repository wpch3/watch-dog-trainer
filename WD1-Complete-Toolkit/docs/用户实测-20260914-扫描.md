# 用户只读实测 — 2026-09-14

来自 WD1-Native-Probe.json.txt，工具0.3.2，UTC 07:19:05。

- connected=true；COMPLETE_READONLY_SNAPSHOT；Complete=true。
- 三项指纹、进程路径、引擎路径均匹配。磁盘/运行时PE头一致。
- 模块106516480字节（101.582031MiB），可执行节85279343字节（81.328719MiB）。所有节末端均在模块范围内。
- 旧错误已可确定：累计可执行节超过64MiB上限；不是报告模块大小失配、用户目录/站位错误。
- 实际读85254767字节；跳过24576字节（24KiB）当前RW而非执行页；44个块，无读失败。跳过页面是设计决策，不是损坏。
- 14个指令定位项：12个唯一且原字节匹配；inventory_decrement和drink_b为0。
- 所有24个功能控件都关闭；没有对象捕获值/修改效果/持久存档验证。

当前可升级为“用户真实进程只读定位通过”，不能升级为“对应修改功能已验证”。
无限库存资源仍因inventory_decrement未命中而不能启用；饮酒计时要求drink_a和drink_b两处，不能只因第一处命中就放行。保留这些功能，不用增加资源替代无限。

下一步仅在有效备份和单机条件下，逐项核对捕获对象，例如当前现金是否与游戏一致，再决定小额一次性测试；不要全开。无需再重复确认路径或重新扫描同一状态。
