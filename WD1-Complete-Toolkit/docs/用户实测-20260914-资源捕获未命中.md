> **后续纠正：**用户随后用0.3.3完整执行“排队增加50000 → ATM取款”，现金从约210000变为260000，报告加钱成功，且未安装NexusTools。以下未命中仅是当时导出快照，不能解释为资源入口永久不可用。先前建议因入口不可用而转Lua的结论过早；原生金钱路线可继续逐项验证。见 `profiles/steam-11241563-cash-success-20260914.json`。

# 资源捕获实测未命中

来源：WD1-Native-Probe2.json.txt，0.3.3。

完整只读扫描成功；resources仍唯一且原字节匹配。资源捕获Installed/HookIntact/BufferReadable/Coherent均true，但HitCount=0、PointerObserved=false、StableSamples=0、SecondsSinceHit=null、Value=null。

因此当前阻碍不是2秒过期，也不是用户“先捕获再买东西”的顺序错误；该资源绑定未产生任何有效捕获。仅凭遥测不能进一步断定是未执行代码路径还是捕获桩/跳转执行链的问题。禁止把缓存/范围保护放宽以写入空对象，也不将备用固定偏移当已验证修复。

受影响的五个控件共用resources：money_floor、skill_floor、cash_add、skill_add、xp_add。当前不能将它们视为已适配功能。

相较上一份报告，battery与reload由1次精确命中变为0；其他已匹配RVA保持一致。新报告当前为10/14项唯一精确匹配。它不能证明变化是其他修改器、旧版残留、宿主还是游戏运行时本身造成，不能责怪用户或把磁盘哈希正常当作内存未改的证据。

不要求继续重复花钱/赚钱；先安全撤销当前捕获，核实运行中是否有其他修改来源及宿主状态，再选择实际资源入口或已核对的Lua语义接口继续。完整功能计划保留，未实测写入不升级为可用。

## 用户进一步确认

- 同一游戏进程只使用当前0.3.3，没有其他修改器/旧版本。不能以其他修改器干扰为既定原因。
- 尚未安装NexusTools，游戏内Lua路线未开始。
- 原生资源入口/捕获链必须重新核查；现阶段优先使用已有且有来源的Lua语义接口路线做小额核验，不把“已安装宿主”说成“修改功能已验证”。官方主页当前列1.1.12，手动ZIP解压游戏根目录，见 https://www.kaverti.com/en/TroploNexusTools/Home 。
